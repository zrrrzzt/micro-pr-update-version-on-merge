{
  "name": "micro-pr-update-version-on-merge",
  "description": "Listen for pr-merges and update version number",
  "version": "1.2.2",
  "license": "MIT",
  "private": true,
  "author": {
    "name": "Geir Gåsodden",
    "email": "geir.gasodden@pythonia.no",
    "url": "https://github.com/zrrrzzt"
  },
  "main": "index.js",
  "engines": {
    "node": ">=7.10.0"
  },
  "scripts": {
    "test": "standard && nsp check && ava",
    "test-offline": "standard && ava",
    "coverage": "nyc ava",
    "coveralls": "nyc ava && nyc report --reporter=lcov && cat coverage/lcov.info | coveralls",
    "setup": "npm install",
    "standard-fix": "standard --fix",
    "start": "micro",
    "now-deploy": "npm test && now --npm -E tfk.env && now alias"
  },
  "keywords": [
    "microservice",
    "micro",
    "github"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/zrrrzzt/micro-pr-update-version-on-merge.git"
  },
  "bugs": {
    "url": "https://github.com/zrrrzzt/micro-pr-update-version-on-merge/issues"
  },
  "homepage": "https://github.com/zrrrzzt/micro-pr-update-version-on-merge",
  "now": {
    "alias": [
      "update.version.github.tjeneste.win"
    ],
    "type": "npm"
  },
  "devDependencies": {
    "ava": "0.22.0",
    "coveralls": "2.13.1",
    "nsp": "2.7.0",
    "nyc": "11.2.0",
    "standard": "10.0.3",
    "test-listen": "1.0.2"
  },
  "dependencies": {
    "axios": "0.16.2",
    "marked": "0.3.6",
    "micro": "8.0.4",
    "semver": "5.4.1"
  }
}
